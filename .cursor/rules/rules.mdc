---
alwaysApply: true
---

Read all MD files

The deep_agents_from_scratch directory under /src is the code for foundational architecture, intended for production. Remind user if anything falls short of production standard.

Import statements always on top of the file.

When adding a new agent to agents folder, add it to the agent_registry
